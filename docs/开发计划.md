# Keyboard Tracker 详细开发方案

## 阶段 1：项目初始化

### 1.1 创建项目

```bash
# 创建 Tauri + React + TypeScript 项目
npm create tauri-app@latest keyboard-tracker -- --template react-ts

# 安装前端依赖
cd keyboard-tracker
npm install zustand recharts framer-motion dayjs
npm install -D @types/node
```

### 1.2 配置 Rust 依赖

**Cargo.toml**

```toml
[dependencies]
tauri = { version = "2", features = ["tray-icon", "macos-private-api"] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
rusqlite = { version = "0.31", features = ["bundled"] }
rdev = "0.5"
dirs = "5"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["winuser", "processthreadsapi", "psapi"] }

[target.'cfg(target_os = "macos")'.dependencies]
core-graphics = "0.24"
cocoa = "0.26"
objc = "0.2"
core-foundation = "0.9"
```

### 1.3 Tauri 配置

**tauri.conf.json**

```json
{
  "productName": "KeyboardTracker",
  "identifier": "com.yourname.keyboard-tracker",
  "version": "0.1.0",
  "build": { "frontendDist": "../dist" },
  "app": {
    "trayIcon": {
      "iconPath": "icons/icon.png",
      "iconAsTemplate": true
    },
    "windows": [
      {
        "label": "main",
        "title": "Keyboard Tracker",
        "width": 900,
        "height": 600,
        "visible": false,
        "resizable": true
      },
      {
        "label": "floating",
        "title": "Stats Widget",
        "width": 220,
        "height": 160,
        "decorations": false,
        "alwaysOnTop": true,
        "skipTaskbar": true,
        "visible": true,
        "transparent": true
      }
    ],
    "security": {
      "csp": null
    }
  }
}
```

### 1.4 项目目录结构

```
keyboard-tracker/
├── src/                          # React 前端
│   ├── App.tsx
│   ├── main.tsx
│   ├── components/
│   │   ├── FloatingWidget/
│   │   │   ├── index.tsx
│   │   │   ├── HeartbeatGauge.tsx
│   │   │   └── styles.module.css
│   │   ├── Dashboard/
│   │   │   ├── index.tsx
│   │   │   ├── DailyChart.tsx
│   │   │   ├── AppBreakdown.tsx
│   │   │   └── KeyTypeStats.tsx
│   │   ├── SalaryCalc/
│   │   │   ├── index.tsx
│   │   │   ├── SalaryForm.tsx
│   │   │   └── RealtimeIncome.tsx
│   │   └── Settings/
│   │       ├── index.tsx
│   │       ├── GoalSettings.tsx
│   │       └── PrivacySettings.tsx
│   ├── hooks/
│   │   ├── useKeystrokeStats.ts
│   │   ├── useSalary.ts
│   │   └── useSettings.ts
│   ├── stores/
│   │   ├── keystrokeStore.ts
│   │   ├── salaryStore.ts
│   │   └── settingsStore.ts
│   ├── types/
│   │   ├── keystroke.ts
│   │   ├── salary.ts
│   │   └── settings.ts
│   └── styles/
│       └── global.css
├── src-tauri/
│   ├── src/
│   │   ├── main.rs
│   │   ├── lib.rs
│   │   ├── keyboard/
│   │   │   ├── mod.rs
│   │   │   ├── listener.rs          # 键盘监听
│   │   │   └── key_classifier.rs    # 按键分类
│   │   ├── window/
│   │   │   ├── mod.rs
│   │   │   ├── windows.rs           # Windows 实现
│   │   │   └── macos.rs             # macOS 实现
│   │   ├── database/
│   │   │   ├── mod.rs
│   │   │   ├── schema.rs            # 表结构
│   │   │   └── operations.rs        # CRUD 操作
│   │   ├── salary/
│   │   │   ├── mod.rs
│   │   │   └── calculator.rs        # 工资计算
│   │   └── commands/
│   │       ├── mod.rs
│   │       ├── keystroke.rs
│   │       ├── heartbeat.rs         # 心跳数据
│   │       ├── salary.rs
│   │       └── settings.rs
│   ├── Cargo.toml
│   └── tauri.conf.json
└── package.json
```

---

## 阶段 2：键盘全局监听

### 2.1 按键类型定义

**src-tauri/src/keyboard/key_classifier.rs**

```rust
use serde::Serialize;

#[derive(Debug, Clone, Serialize, PartialEq)]
pub enum KeyType {
    Char,        // 字符键: a-z, 0-9, 符号
    Function,    // 功能键: F1-F12
    Modifier,    // 修饰键: Ctrl, Alt, Shift, Cmd
    Navigation,  // 导航键: 方向键, Home, End, PageUp/Down
    Editing,     // 编辑键: Backspace, Delete, Insert
    System,      // 系统键: Esc, Tab, Enter, Space
}

#[derive(Debug, Clone, Serialize)]
pub struct KeyEvent {
    pub key_code: u32,
    pub key_name: String,
    pub key_type: KeyType,
    pub timestamp: i64,
    pub is_key_down: bool,
}

pub fn classify_key(key_code: u32, key_name: &str) -> KeyType {
    match key_code {
        // 字母和数字
        0x04..=0x1D | 0x1E..=0x27 if is_char_key(key_name) => KeyType::Char,
        // F1-F12
        0x3A..=0x45 => KeyType::Function,
        // Shift, Ctrl, Alt, Cmd
        0xE0..=0xE7 => KeyType::Modifier,
        // 方向键
        0x4F..=0x52 => KeyType::Navigation,
        // Backspace, Delete
        0x2A | 0x4C => KeyType::Editing,
        // Enter, Tab, Space, Esc
        0x28 | 0x2B | 0x2C | 0x29 => KeyType::System,
        _ => KeyType::Char,
    }
}

fn is_char_key(name: &str) -> bool {
    name.len() == 1 && (name.is_alphanumeric() || name.is_ascii_punctuation())
}
```

### 2.2 键盘监听器

**src-tauri/src/keyboard/listener.rs**

```rust
use rdev::{listen, Event, EventType, Key};
use std::sync::{Arc, Mutex};
use tauri::{AppHandle, Emitter};
use crate::keyboard::key_classifier::{classify_key, KeyEvent, KeyType};

pub struct KeyboardListener {
    app_handle: AppHandle,
    is_running: Arc<Mutex<bool>>,
    buffer: Arc<Mutex<Vec<KeyEvent>>>,
}

impl KeyboardListener {
    pub fn new(app_handle: AppHandle) -> Self {
        Self {
            app_handle,
            is_running: Arc::new(Mutex::new(false)),
            buffer: Arc::new(Mutex::new(Vec::with_capacity(100))),
        }
    }

    pub fn start(&self) -> Result<(), Box<dyn std::error::Error>> {
        *self.is_running.lock().unwrap() = true;

        let app_handle = self.app_handle.clone();
        let is_running = self.is_running.clone();
        let buffer = self.buffer.clone();

        // 在独立线程中监听
        std::thread::spawn(move || {
            let callback = move |event: Event| {
                let running = is_running.lock().unwrap();
                if !*running {
                    return;
                }
                drop(running);

                if let EventType::KeyPress(key) = event.event_type {
                    let key_name = key_to_string(&key);
                    let key_code = key as u32;
                    let key_type = classify_key(key_code, &key_name);

                    let key_event = KeyEvent {
                        key_code,
                        key_name,
                        key_type,
                        timestamp: chrono::Utc::now().timestamp_millis(),
                        is_key_down: true,
                    };

                    // 发送到前端
                    let _ = app_handle.emit("keystroke", &key_event);

                    // 缓存到批量写入缓冲区
                    let mut buf = buffer.lock().unwrap();
                    buf.push(key_event);
                    if buf.len() >= 50 {
                        // 触发批量写入数据库
                        let _ = app_handle.emit("keystroke-batch", buf.clone());
                        buf.clear();
                    }
                }
            };

            // 注意：rdev::listen 会阻塞当前线程
            if let Err(e) = listen(callback) {
                eprintln!("键盘监听错误: {:?}", e);
            }
        });

        Ok(())
    }

    pub fn stop(&self) {
        *self.is_running.lock().unwrap() = false;
    }
}

fn key_to_string(key: &Key) -> String {
    match key {
        Key::KeyA => "a".to_string(),
        Key::KeyB => "b".to_string(),
        // ... 其他按键映射
        Key::Space => "Space".to_string(),
        Key::Return => "Enter".to_string(),
        Key::Backspace => "Backspace".to_string(),
        _ => format!("{:?}", key),
    }
}
```

### 2.3 macOS 权限处理

**src-tauri/src/keyboard/macos.rs**

```rust
#[cfg(target_os = "macos")]
pub fn check_accessibility_permission() -> bool {
    use std::process::Command;

    let output = Command::new("osascript")
        .arg("-e")
        .arg("tell application \"System Events\" to get name of every process")
        .output();

    output.is_ok()
}

#[cfg(target_os = "macos")]
pub fn request_accessibility_permission() {
    // 打开系统偏好设置的辅助功能页面
    let _ = std::process::Command::new("open")
        .arg("x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility")
        .spawn();
}
```

---

## 阶段 3：活动窗口检测

### 3.1 跨平台抽象

**src-tauri/src/window/mod.rs**

```rust
use serde::Serialize;

#[derive(Debug, Clone, Serialize)]
pub struct ActiveWindow {
    pub app_name: String,
    pub window_title: String,
    pub process_id: u32,
    pub category: AppCategory,
}

#[derive(Debug, Clone, Serialize, PartialEq)]
pub enum AppCategory {
    IDE,       // VSCode, IDEA, Cursor
    Browser,   // Chrome, Edge, Safari
    Terminal,  // Terminal, iTerm, CMD
    Chat,      // WeChat, Slack, Discord
    Office,    // Word, Excel, PowerPoint
    Other,
}

#[cfg(target_os = "windows")]
mod windows;

#[cfg(target_os = "macos")]
mod macos;

pub fn get_active_window() -> Option<ActiveWindow> {
    #[cfg(target_os = "windows")]
    return windows::get_active_window();

    #[cfg(target_os = "macos")]
    return macos::get_active_window();
}

pub fn classify_app(app_name: &str) -> AppCategory {
    let app_lower = app_name.to_lowercase();

    if ["vscode", "idea", "cursor", "sublime", "atom", "vim", "neovim", "xcode"]
        .iter().any(|s| app_lower.contains(s)) {
        AppCategory::IDE
    } else if ["chrome", "edge", "firefox", "safari", "brave"]
        .iter().any(|s| app_lower.contains(s)) {
        AppCategory::Browser
    } else if ["terminal", "iterm", "cmd", "powershell", "windowsterminal"]
        .iter().any(|s| app_lower.contains(s)) {
        AppCategory::Terminal
    } else if ["wechat", "slack", "discord", "teams", "qq", "telegram"]
        .iter().any(|s| app_lower.contains(s)) {
        AppCategory::Chat
    } else if ["word", "excel", "powerpoint", "notion", "obsidian"]
        .iter().any(|s| app_lower.contains(s)) {
        AppCategory::Office
    } else {
        AppCategory::Other
    }
}
```

### 3.2 Windows 实现

**src-tauri/src/window/windows.rs**

```rust
use winapi::um::winuser::{GetForegroundWindow, GetWindowTextW, GetWindowThreadProcessId};
use winapi::um::processthreadsapi::OpenProcess;
use winapi::um::psapi::GetProcessImageFileNameW;
use winapi::um::winnt::{PROCESS_QUERY_INFORMATION, PROCESS_VM_READ};
use std::ffi::OsString;
use std::os::windows::ffi::OsStringExt;
use super::{ActiveWindow, classify_app, AppCategory};

pub fn get_active_window() -> Option<ActiveWindow> {
    unsafe {
        let hwnd = GetForegroundWindow();
        if hwnd.is_null() {
            return None;
        }

        // 获取窗口标题
        let mut title: [u16; 512] = [0; 512];
        let title_len = GetWindowTextW(hwnd, title.as_mut_ptr(), 512);
        let window_title = String::from_utf16_lossy(&title[..title_len as usize]);

        // 获取进程ID
        let mut process_id: u32 = 0;
        GetWindowThreadProcessId(hwnd, &mut process_id);

        // 获取进程名
        let process_handle = OpenProcess(PROCESS_QUERY_INFORMATION | PROCESS_VM_READ, 0, process_id);
        if process_handle.is_null() {
            return None;
        }

        let mut process_name: [u16; 512] = [0; 512];
        let name_len = GetProcessImageFileNameW(process_handle, process_name.as_mut_ptr(), 512);
        let full_name = OsString::from_wide(&process_name[..name_len as usize]);

        let app_name = full_name
            .to_string_lossy()
            .split('\\')
            .last()
            .unwrap_or("Unknown")
            .replace(".exe", "");

        let category = classify_app(&app_name);

        Some(ActiveWindow {
            app_name,
            window_title,
            process_id,
            category,
        })
    }
}
```

### 3.3 macOS 实现

**src-tauri/src/window/macos.rs**

```rust
use cocoa::appkit::{NSWorkspace, NSWorkspaceActiveApplicationKey};
use cocoa::base::{id, nil};
use cocoa::foundation::{NSDictionary, NSString};
use std::ffi::CStr;
use std::os::raw::c_char;
use super::{ActiveWindow, classify_app, AppCategory};

/// 获取当前活动窗口信息
pub fn get_active_window() -> Option<ActiveWindow> {
    unsafe {
        // 获取共享工作空间
        let workspace: id = NSWorkspace::sharedWorkspace();

        // 获取当前激活的应用程序字典
        let app_dict: id = workspace.activeApplication();

        if app_dict.is_null() {
            return None;
        }

        // 提取应用名称
        let app_name = extract_string_from_dict(app_dict, NSWorkspaceActiveApplicationKey);

        // 提取进程 ID
        let process_id = extract_pid_from_dict(app_dict);

        // 提取窗口标题 (需要辅助功能权限)
        let window_title = get_window_title(process_id).unwrap_or_default();

        // 分类应用
        let category = classify_app(&app_name);

        Some(ActiveWindow {
            app_name,
            window_title,
            process_id,
            category,
        })
    }
}

/// 从字典中提取字符串值
unsafe fn extract_string_from_dict(dict: id, key: *const i8) -> String {
    let key_str = NSString::alloc(nil).init_str(key);
    let value: id = msg_send![dict, objectForKey: key_str];

    if value.is_null() {
        return String::new();
    }

    let c_str: *const c_char = msg_send![value, UTF8String];
    if c_str.is_null() {
        return String::new();
    }

    CStr::from_ptr(c_str).to_string_lossy().into_owned()
}

/// 从字典中提取进程 ID
unsafe fn extract_pid_from_dict(dict: id) -> u32 {
    let pid_key = b"NSApplicationProcessIdentifier\0".as_ptr() as *const i8;
    let key_str = NSString::alloc(nil).init_str(pid_key);
    let value: id = msg_send![dict, objectForKey: key_str];

    if value.is_null() {
        return 0;
    }

    let pid: i32 = msg_send![value, intValue];
    pid as u32
}

/// 获取窗口标题 (需要辅助功能权限)
unsafe fn get_window_title(pid: u32) -> Option<String> {
    use core_graphics::window::{
        kCGNullWindowID,
        kCGWindowListOptionOnScreenOnly,
        CGWindowListCopyWindowInfo,
    };

    // 获取所有窗口信息
    let window_list = CGWindowListCopyWindowInfo(
        kCGWindowListOptionOnScreenOnly,
        kCGNullWindowID,
    );

    if window_list.is_null() {
        return None;
    }

    // 遍历窗口列表查找匹配的进程
    let count: usize = msg_send![window_list, count];
    for i in 0..count {
        let window: id = msg_send![window_list, objectAtIndex: i];

        // 获取窗口所属进程 ID
        let pid_key = b"kCGWindowOwnerPID\0".as_ptr() as *const i8;
        let key_str = NSString::alloc(nil).init_str(pid_key);
        let owner_pid: id = msg_send![window, objectForKey: key_str];
        let owner_pid: i32 = msg_send![owner_pid, intValue];

        if owner_pid as u32 == pid {
            // 获取窗口标题
            let title_key = b"kCGWindowName\0".as_ptr() as *const i8;
            let key_str = NSString::alloc(nil).init_str(title_key);
            let title: id = msg_send![window, objectForKey: key_str];

            if !title.is_null() {
                let c_str: *const c_char = msg_send![title, UTF8String];
                if !c_str.is_null() {
                    return Some(CStr::from_ptr(c_str).to_string_lossy().into_owned());
                }
            }
            break;
        }
    }

    // 释放窗口列表
    let _: () = msg_send![window_list, release];

    None
}

/// 检查辅助功能权限
pub fn check_accessibility_permission() -> bool {
    use core_foundation::boolean::CFBoolean;

    unsafe {
        let options = NSDictionary::dictionaryWithObject_forKey_(
            nil,
            CFBoolean::true_value() as id,
            b"AXTrustedCheckOptionPrompt\0".as_ptr() as *const i8,
        );

        AXIsProcessTrustedWithOptions(options)
    }
}

/// 请求辅助功能权限
pub fn request_accessibility_permission() {
    use core_foundation::boolean::CFBoolean;

    unsafe {
        let options = NSDictionary::dictionaryWithObject_forKey_(
            nil,
            CFBoolean::true_value() as id,
            b"AXTrustedCheckOptionPrompt\0".as_ptr() as *const i8,
        );

        // 这会弹出系统权限请求对话框
        AXIsProcessTrustedWithOptions(options);
    }
}
```

### 3.4 Code/Talk Ratio "心跳" 功能

**src-tauri/src/commands/heartbeat.rs**

```rust
use serde::{Deserialize, Serialize};

/// 心跳数据统计
#[derive(Debug, Clone, Serialize)]
pub struct HeartbeatData {
    /// Code 按键数 (IDE + Terminal)
    pub code_keystrokes: u64,
    /// Talk 按键数 (Chat 应用)
    pub talk_keystrokes: u64,
    /// 其他应用按键数
    pub other_keystrokes: u64,
    /// Code 比例 (0-100)
    pub code_ratio: f32,
    /// Talk 比例 (0-100)
    pub talk_ratio: f32,
    /// 当前状态
    pub status: WorkStatus,
    /// 心跳频率 (BPM) - 编码时快，摸鱼时慢
    pub heartbeat_bpm: u32,
}

#[derive(Debug, Clone, Serialize, PartialEq)]
pub enum WorkStatus {
    /// 编码中
    Coding,
    /// 聊天中
    Talking,
    /// 摸鱼中
    Idle,
    /// 平衡状态
    Balanced,
}

impl HeartbeatData {
    /// 从数据库统计生成心跳数据
    pub fn from_stats(code: u64, talk: u64, other: u64) -> Self {
        let total = code + talk + other;
        let code_ratio = if total > 0 { (code as f32 / total as f32) * 100.0 } else { 0.0 };
        let talk_ratio = if total > 0 { (talk as f32 / total as f32) * 100.0 } else { 0.0 };

        // 判断状态
        let status = if code_ratio > 60.0 {
            WorkStatus::Coding
        } else if talk_ratio > 40.0 {
            WorkStatus::Talking
        } else if total == 0 {
            WorkStatus::Idle
        } else {
            WorkStatus::Balanced
        };

        // 计算心跳频率：编码时 80-120，摸鱼时 40-60
        let heartbeat_bpm = match status {
            WorkStatus::Coding => 80 + ((code_ratio - 60.0) * 0.5) as u32,
            WorkStatus::Talking => 60 + ((talk_ratio - 20.0) * 0.3) as u32,
            WorkStatus::Idle => 40,
            WorkStatus::Balanced => 70,
        };

        Self {
            code_keystrokes: code,
            talk_keystrokes: talk,
            other_keystrokes: other,
            code_ratio,
            talk_ratio,
            status,
            heartbeat_bpm: heartbeat_bpm.min(120),
        }
    }
}

/// Tauri 命令：获取心跳数据
#[tauri::command]
pub fn get_heartbeat_data(db: tauri::State<'_, Database>) -> Result<HeartbeatData, String> {
    let stats = db.get_category_stats_today()
        .map_err(|e| e.to_string())?;

    Ok(HeartbeatData::from_stats(
        stats.ide_count + stats.terminal_count,
        stats.chat_count,
        stats.other_count,
    ))
}
```

---

## 阶段 4：数据库设计

### 4.1 数据库 Schema

**src-tauri/src/database/schema.rs**

```rust
pub const SCHEMA: &str = r#"
-- 按键记录表 (主表)
CREATE TABLE IF NOT EXISTS keystrokes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp INTEGER NOT NULL,           -- Unix 毫秒时间戳
    key_code INTEGER NOT NULL,
    key_name TEXT NOT NULL,
    key_type TEXT NOT NULL,               -- char/function/modifier/navigation/editing/system
    app_name TEXT,
    app_category TEXT,                    -- ide/browser/terminal/chat/office/other
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 按键时间索引 (查询速度优化)
CREATE INDEX IF NOT EXISTS idx_keystrokes_timestamp ON keystrokes(timestamp);
CREATE INDEX IF NOT EXISTS idx_keystrokes_app_category ON keystrokes(app_category);

-- 每日统计汇总表 (减少查询压力)
CREATE TABLE IF NOT EXISTS daily_stats (
    date TEXT PRIMARY KEY,                -- YYYY-MM-DD
    total_keystrokes INTEGER DEFAULT 0,
    char_count INTEGER DEFAULT 0,
    code_keystrokes INTEGER DEFAULT 0,    -- IDE中的按键数
    browser_keystrokes INTEGER DEFAULT 0,
    terminal_keystrokes INTEGER DEFAULT 0,
    chat_keystrokes INTEGER DEFAULT 0,
    other_keystrokes INTEGER DEFAULT 0,
    work_duration INTEGER DEFAULT 0,      -- 工作时长(秒)
    goal_achieved BOOLEAN DEFAULT FALSE
);

-- 小时统计表 (生成图表用)
CREATE TABLE IF NOT EXISTS hourly_stats (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    date TEXT NOT NULL,                   -- YYYY-MM-DD
    hour INTEGER NOT NULL,                -- 0-23
    keystroke_count INTEGER DEFAULT 0,
    UNIQUE(date, hour)
);

-- 工资配置表
CREATE TABLE IF NOT EXISTS salary_config (
    id INTEGER PRIMARY KEY CHECK (id = 1), -- 只允许一条记录
    monthly_salary REAL NOT NULL DEFAULT 0,
    work_days_per_month INTEGER DEFAULT 22,
    work_hours_per_day REAL DEFAULT 8,
    work_start_time TEXT DEFAULT '09:00',  -- HH:MM
    work_end_time TEXT DEFAULT '18:00',
    currency TEXT DEFAULT 'CNY'
);

-- 目标设置表
CREATE TABLE IF NOT EXISTS goal_settings (
    id INTEGER PRIMARY KEY CHECK (id = 1),
    daily_keystroke_goal INTEGER DEFAULT 10000,
    enable_reminder BOOLEAN DEFAULT TRUE,
    reminder_interval INTEGER DEFAULT 30,  -- 分钟
    pause_during_sensitive BOOLEAN DEFAULT TRUE
);

-- 应用分类自定义表
CREATE TABLE IF NOT EXISTS app_categories (
    app_name TEXT PRIMARY KEY,
    category TEXT NOT NULL
);

-- 初始化默认配置
INSERT OR IGNORE INTO salary_config (id) VALUES (1);
INSERT OR IGNORE INTO goal_settings (id) VALUES (1);
"#;
```

### 4.2 数据库操作

**src-tauri/src/database/operations.rs**

```rust
use rusqlite::{Connection, params};
use crate::keyboard::KeyEvent;
use crate::window::AppCategory;
use chrono::{DateTime, Utc, Datelike, Timelike};
use serde::Serialize;

pub struct Database {
    conn: Connection,
}

impl Database {
    pub fn new() -> Result<Self, rusqlite::Error> {
        let db_path = dirs::data_dir()
            .unwrap()
            .join("keyboard-tracker")
            .join("data.db");

        std::fs::create_dir_all(db_path.parent().unwrap()).ok();
        let conn = Connection::open(db_path)?;
        conn.execute_batch(crate::database::schema::SCHEMA)?;

        Ok(Self { conn })
    }

    /// 批量插入按键记录
    pub fn insert_keystrokes(
        &self,
        events: &[KeyEvent],
        app_name: &str,
        app_category: &AppCategory
    ) -> Result<(), rusqlite::Error> {
        let tx = self.conn.transaction()?;

        for event in events {
            tx.execute(
                "INSERT INTO keystrokes (timestamp, key_code, key_name, key_type, app_name, app_category)
                 VALUES (?1, ?2, ?3, ?4, ?5, ?6)",
                params![
                    event.timestamp,
                    event.key_code,
                    event.key_name,
                    format!("{:?}", event.key_type),
                    app_name,
                    format!("{:?}", app_category),
                ],
            )?;
        }

        tx.commit()
    }

    /// 获取今日统计
    pub fn get_today_stats(&self) -> Result<TodayStats, rusqlite::Error> {
        let today = Utc::now().format("%Y-%m-%d").to_string();

        self.conn.query_row(
            "SELECT COUNT(*),
                    SUM(CASE WHEN key_type = 'Char' THEN 1 ELSE 0 END),
                    SUM(CASE WHEN app_category = 'IDE' THEN 1 ELSE 0 END),
                    SUM(CASE WHEN app_category = 'Browser' THEN 1 ELSE 0 END)
             FROM keystrokes
             WHERE date(timestamp/1000, 'unixepoch') = ?1",
            params![today],
            |row| Ok(TodayStats {
                total: row.get(0)?,
                char_count: row.get(1)?,
                code_count: row.get(2)?,
                browser_count: row.get(3)?,
            }),
        )
    }

    /// 获取今日分时统计
    pub fn get_today_hourly(&self) -> Result<Vec<HourlyStats>, rusqlite::Error> {
        let today = Utc::now().format("%Y-%m-%d").to_string();

        let mut stmt = self.conn.prepare(
            "SELECT strftime('%H', datetime(timestamp/1000, 'unixepoch')) as hour, COUNT(*)
             FROM keystrokes
             WHERE date(timestamp/1000, 'unixepoch') = ?1
             GROUP BY hour
             ORDER BY hour"
        )?;

        let stats = stmt.query_map(params![today], |row| {
            Ok(HourlyStats {
                hour: row.get(0)?,
                count: row.get(1)?,
            })
        })?.collect::<Result<Vec<_>, _>>()?;

        Ok(stats)
    }

    /// 获取历史统计 (最近N天)
    pub fn get_history_stats(&self, days: i32) -> Result<Vec<DailyStats>, rusqlite::Error> {
        let mut stmt = self.conn.prepare(
            "SELECT date(timestamp/1000, 'unixepoch') as date, COUNT(*)
             FROM keystrokes
             WHERE date(timestamp/1000, 'unixepoch') >= date('now', ?1)
             GROUP BY date
             ORDER BY date"
        )?;

        let stats = stmt.query_map(
            params![format!("-{} days", days)],
            |row| Ok(DailyStats {
                date: row.get(0)?,
                total: row.get(1)?,
            })
        )?.collect::<Result<Vec<_>, _>>()?;

        Ok(stats)
    }

    /// 更新每日汇总表
    pub fn update_daily_summary(&self, date: &str) -> Result<(), rusqlite::Error> {
        self.conn.execute(
            "INSERT OR REPLACE INTO daily_stats (date, total_keystrokes, char_count, code_keystrokes)
             SELECT
                ?1 as date,
                COUNT(*) as total,
                SUM(CASE WHEN key_type = 'Char' THEN 1 ELSE 0 END) as chars,
                SUM(CASE WHEN app_category = 'IDE' THEN 1 ELSE 0 END) as code
             FROM keystrokes
             WHERE date(timestamp/1000, 'unixepoch') = ?1",
            params![date],
        )?;
        Ok(())
    }
}

#[derive(Debug, Serialize)]
pub struct TodayStats {
    pub total: i64,
    pub char_count: i64,
    pub code_count: i64,
    pub browser_count: i64,
}

#[derive(Debug, Serialize)]
pub struct HourlyStats {
    pub hour: String,
    pub count: i64,
}

#[derive(Debug, Serialize)]
pub struct DailyStats {
    pub date: String,
    pub total: i64,
}
```

---

## 阶段 5：悬浮窗 UI (毛玻璃极简风 + 心跳仪表盘)

### 5.1 悬浮窗组件

**src/components/FloatingWidget/index.tsx**

```tsx
import { useEffect, useState, useMemo } from 'react';
import { invoke } from '@tauri-apps/api/core';
import { listen } from '@tauri-apps/api/event';
import { appWindow } from '@tauri-apps/api/window';
import { motion } from 'framer-motion';
import { HeartbeatGauge } from './HeartbeatGauge';
import styles from './styles.module.css';

interface WidgetData {
  todayKeystrokes: number;
  todayEarnings: number;
  countdown: string;
  goalProgress: number;
}

interface HeartbeatData {
  code_keystrokes: number;
  talk_keystrokes: number;
  other_keystrokes: number;
  code_ratio: number;
  talk_ratio: number;
  status: 'Coding' | 'Talking' | 'Idle' | 'Balanced';
  heartbeat_bpm: number;
}

export function FloatingWidget() {
  const [data, setData] = useState<WidgetData>({
    todayKeystrokes: 0,
    todayEarnings: 0,
    countdown: '00:00:00',
    goalProgress: 0,
  });

  const [heartbeat, setHeartbeat] = useState<HeartbeatData>({
    code_keystrokes: 0,
    talk_keystrokes: 0,
    other_keystrokes: 0,
    code_ratio: 0,
    talk_ratio: 0,
    status: 'Idle',
    heartbeat_bpm: 40,
  });

  const [isDragging, setIsDragging] = useState(false);

  useEffect(() => {
    const unlisten = listen('keystroke', () => {
      updateStats();
      updateHeartbeat();
    });

    const timer = setInterval(() => {
      updateCountdown();
    }, 1000);

    updateStats();
    updateHeartbeat();

    return () => {
      unlisten.then(fn => fn());
      clearInterval(timer);
    };
  }, []);

  async function updateStats() {
    const stats = await invoke<{ total: number }>('get_today_stats');
    const salary = await invoke<{ daily: number }>('get_salary_info');
    const goal = await invoke<{ daily: number }>('get_goal_settings');

    setData(prev => ({
      ...prev,
      todayKeystrokes: stats.total,
      todayEarnings: (salary.daily / 8) * (new Date().getHours() - 9),
      goalProgress: Math.min((stats.total / goal.daily) * 100, 100),
    }));
  }

  async function updateHeartbeat() {
    const hb = await invoke<HeartbeatData>('get_heartbeat_data');
    setHeartbeat(hb);
  }

  async function updateCountdown() {
    const config = await invoke<{ workEnd: string }>('get_work_schedule');
    const [endHour, endMin] = config.workEnd.split(':').map(Number);

    const now = new Date();
    const end = new Date();
    end.setHours(endHour, endMin, 0, 0);

    const diff = end.getTime() - now.getTime();

    if (diff > 0) {
      const hours = Math.floor(diff / 3600000);
      const mins = Math.floor((diff % 3600000) / 60000);
      const secs = Math.floor((diff % 60000) / 1000);

      setData(prev => ({
        ...prev,
        countdown: `${hours.toString().padStart(2, '0')}:${mins.toString().padStart(2, '0')}:${secs.toString().padStart(2, '0')}`,
      }));
    }
  }

  async function handleMouseDown(e: React.MouseEvent) {
    setIsDragging(true);
    await appWindow.startDragging();
    setIsDragging(false);
  }

  const statusEmoji = useMemo(() => {
    switch (heartbeat.status) {
      case 'Coding': return '💻';
      case 'Talking': return '💬';
      case 'Idle': return '🐟';
      case 'Balanced': return '⚖️';
    }
  }, [heartbeat.status]);

  return (
    <motion.div
      className={styles.widget}
      initial={{ opacity: 0, scale: 0.9, y: 20 }}
      animate={{ opacity: 1, scale: 1, y: 0 }}
      transition={{ type: 'spring', stiffness: 300, damping: 25 }}
      onMouseDown={handleMouseDown}
      style={{ cursor: isDragging ? 'grabbing' : 'grab' }}
    >
      {/* 心跳仪表盘区域 */}
      <div className={styles.gaugeSection}>
        <HeartbeatGauge
          codeRatio={heartbeat.code_ratio}
          talkRatio={heartbeat.talk_ratio}
          bpm={heartbeat.heartbeat_bpm}
          status={heartbeat.status}
        />
        <div className={styles.statusBadge}>
          <span className={styles.statusEmoji}>{statusEmoji}</span>
          <span className={styles.bpmText}>{heartbeat.heartbeat_bpm} BPM</span>
        </div>
      </div>

      {/* 统计数据区域 */}
      <div className={styles.statsSection}>
        <div className={styles.statRow}>
          <div className={styles.statIcon}>⌨️</div>
          <div className={styles.statContent}>
            <span className={styles.statLabel}>今日按键</span>
            <span className={styles.statValue}>
              {data.todayKeystrokes.toLocaleString()}
            </span>
          </div>
        </div>

        <div className={styles.statRow}>
          <div className={styles.statIcon}>💰</div>
          <div className={styles.statContent}>
            <span className={styles.statLabel}>今日已赚</span>
            <span className={styles.statValue}>¥{data.todayEarnings.toFixed(2)}</span>
          </div>
        </div>

        <div className={styles.statRow}>
          <div className={styles.statIcon}>⏰</div>
          <div className={styles.statContent}>
            <span className={styles.statLabel}>距下班</span>
            <span className={styles.statValue}>{data.countdown}</span>
          </div>
        </div>
      </div>

      {/* 目标进度条 */}
      <div className={styles.progressSection}>
        <div className={styles.progressHeader}>
          <span className={styles.progressLabel}>今日目标</span>
          <span className={styles.progressPercent}>{Math.round(data.goalProgress)}%</span>
        </div>
        <div className={styles.progressBar}>
          <motion.div
            className={styles.progressFill}
            initial={{ width: 0 }}
            animate={{ width: `${data.goalProgress}%` }}
            transition={{ type: 'spring', stiffness: 100 }}
          />
        </div>
      </div>

      {/* Code/Talk 比例指示 */}
      <div className={styles.ratioSection}>
        <div className={styles.ratioItem}>
          <span className={styles.ratioDot} style={{ background: '#10b981' }} />
          <span className={styles.ratioLabel}>Code</span>
          <span className={styles.ratioValue}>{Math.round(heartbeat.code_ratio)}%</span>
        </div>
        <div className={styles.ratioItem}>
          <span className={styles.ratioDot} style={{ background: '#f59e0b' }} />
          <span className={styles.ratioLabel}>Talk</span>
          <span className={styles.ratioValue}>{Math.round(heartbeat.talk_ratio)}%</span>
        </div>
      </div>
    </motion.div>
  );
}
```

### 5.2 心跳仪表盘组件

**src/components/FloatingWidget/HeartbeatGauge.tsx**

```tsx
import { useMemo } from 'react';
import { motion } from 'framer-motion';
import styles from './styles.module.css';

interface HeartbeatGaugeProps {
  codeRatio: number;
  talkRatio: number;
  bpm: number;
  status: 'Coding' | 'Talking' | 'Idle' | 'Balanced';
}

export function HeartbeatGauge({ codeRatio, talkRatio, bpm, status }: HeartbeatGaugeProps) {
  // 计算指针角度 (0-180度映射到 0-100%)
  const needleAngle = useMemo(() => {
    // 左边是 Talk (0-50)，右边是 Code (50-100)
    const ratio = codeRatio - talkRatio; // -100 到 100
    return (ratio + 100) / 200 * 180 - 90; // 转换为 -90 到 90 度
  }, [codeRatio, talkRatio]);

  const gaugeColor = useMemo(() => {
    switch (status) {
      case 'Coding': return '#10b981'; // 绿色
      case 'Talking': return '#f59e0b'; // 橙色
      case 'Idle': return '#6b7280'; // 灰色
      case 'Balanced': return '#3b82f6'; // 蓝色
    }
  }, [status]);

  // 心跳动画的脉冲效果
  const pulseScale = useMemo(() => {
    return 1 + (bpm - 40) / 200; // 40bpm = 1.0, 120bpm = 1.4
  }, [bpm]);

  return (
    <div className={styles.gaugeContainer}>
      {/* 仪表盘背景 */}
      <svg className={styles.gaugeSvg} viewBox="0 0 120 70">
        {/* 背景弧线 */}
        <path
          d="M 10 60 A 50 50 0 0 1 110 60"
          fill="none"
          stroke="rgba(255,255,255,0.1)"
          strokeWidth="8"
          strokeLinecap="round"
        />

        {/* Talk 区域 (左半) */}
        <path
          d="M 10 60 A 50 50 0 0 1 60 10"
          fill="none"
          stroke="rgba(245, 158, 11, 0.3)"
          strokeWidth="8"
          strokeLinecap="round"
        />

        {/* Code 区域 (右半) */}
        <path
          d="M 60 10 A 50 50 0 0 1 110 60"
          fill="none"
          stroke="rgba(16, 185, 129, 0.3)"
          strokeWidth="8"
          strokeLinecap="round"
        />

        {/* 当前进度弧线 */}
        <motion.path
          d="M 10 60 A 50 50 0 0 1 110 60"
          fill="none"
          stroke={gaugeColor}
          strokeWidth="8"
          strokeLinecap="round"
          strokeDasharray={`${(codeRatio / 100) * 157} 157`}
          initial={{ strokeDasharray: '0 157' }}
          animate={{ strokeDasharray: `${(codeRatio / 100) * 157} 157` }}
          transition={{ type: 'spring', stiffness: 50 }}
        />

        {/* 指针 */}
        <motion.g
          style={{ transformOrigin: '60px 60px' }}
          animate={{ rotate: needleAngle }}
          transition={{ type: 'spring', stiffness: 100 }}
        >
          <line
            x1="60"
            y1="60"
            x2="60"
            y2="20"
            stroke={gaugeColor}
            strokeWidth="2"
            strokeLinecap="round"
          />
          <circle cx="60" cy="60" r="4" fill={gaugeColor} />
        </motion.g>

        {/* 刻度标签 */}
        <text x="10" y="70" className={styles.gaugeLabel}>Talk</text>
        <text x="100" y="70" className={styles.gaugeLabel}>Code</text>
      </svg>

      {/* 中心心跳图标 */}
      <motion.div
        className={styles.heartIcon}
        animate={{ scale: [1, pulseScale, 1] }}
        transition={{ duration: 60 / bpm, repeat: Infinity }}
      >
        ❤️
      </motion.div>
    </div>
  );
}
```

### 5.3 悬浮窗样式 (毛玻璃极简风)

**src/components/FloatingWidget/styles.module.css**

```css
/* === 主容器 - 毛玻璃效果 === */
.widget {
  background: linear-gradient(
    135deg,
    rgba(255, 255, 255, 0.1) 0%,
    rgba(255, 255, 255, 0.05) 100%
  );
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border-radius: 20px;
  padding: 16px;
  color: #fff;
  font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Segoe UI', Roboto, sans-serif;
  font-size: 13px;
  box-shadow:
    0 8px 32px rgba(0, 0, 0, 0.2),
    0 0 0 1px rgba(255, 255, 255, 0.1) inset,
    0 1px 0 rgba(255, 255, 255, 0.1) inset;
  user-select: none;
  min-width: 220px;
  position: relative;
  overflow: hidden;
}

/* 毛玻璃背景光晕效果 */
.widget::before {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(
    ellipse at center,
    rgba(99, 102, 241, 0.1) 0%,
    transparent 70%
  );
  pointer-events: none;
  animation: shimmer 10s ease-in-out infinite;
}

@keyframes shimmer {
  0%, 100% { opacity: 0.5; transform: rotate(0deg); }
  50% { opacity: 0.8; transform: rotate(180deg); }
}

/* === 仪表盘区域 === */
.gaugeSection {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  margin-bottom: 12px;
  padding-bottom: 12px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.gaugeContainer {
  position: relative;
  width: 120px;
  height: 70px;
}

.gaugeSvg {
  width: 100%;
  height: 100%;
}

.gaugeLabel {
  fill: rgba(255, 255, 255, 0.5);
  font-size: 8px;
  font-weight: 500;
}

.heartIcon {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 16px;
  filter: drop-shadow(0 0 8px rgba(255, 100, 100, 0.5));
}

.statusBadge {
  position: absolute;
  right: 0;
  top: 50%;
  transform: translateY(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
}

.statusEmoji {
  font-size: 20px;
}

.bpmText {
  font-size: 10px;
  font-weight: 600;
  color: rgba(255, 255, 255, 0.7);
  background: rgba(0, 0, 0, 0.2);
  padding: 2px 6px;
  border-radius: 4px;
}

/* === 统计数据区域 === */
.statsSection {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-bottom: 12px;
}

.statRow {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 6px 8px;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 10px;
  transition: background 0.2s;
}

.statRow:hover {
  background: rgba(255, 255, 255, 0.1);
}

.statIcon {
  font-size: 16px;
  width: 24px;
  text-align: center;
}

.statContent {
  display: flex;
  justify-content: space-between;
  flex: 1;
}

.statLabel {
  color: rgba(255, 255, 255, 0.6);
  font-size: 12px;
}

.statValue {
  font-weight: 600;
  font-size: 13px;
  font-variant-numeric: tabular-nums;
  background: linear-gradient(135deg, #fff 0%, rgba(255,255,255,0.8) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* === 进度条区域 === */
.progressSection {
  margin-bottom: 12px;
}

.progressHeader {
  display: flex;
  justify-content: space-between;
  margin-bottom: 6px;
}

.progressLabel {
  font-size: 11px;
  color: rgba(255, 255, 255, 0.5);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.progressPercent {
  font-size: 11px;
  font-weight: 600;
  color: #10b981;
}

.progressBar {
  height: 6px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 3px;
  overflow: hidden;
  position: relative;
}

.progressFill {
  height: 100%;
  background: linear-gradient(
    90deg,
    #10b981 0%,
    #34d399 50%,
    #6ee7b7 100%
  );
  border-radius: 3px;
  box-shadow: 0 0 10px rgba(16, 185, 129, 0.5);
  position: relative;
}

/* 进度条闪光效果 */
.progressFill::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: linear-gradient(
    90deg,
    transparent 0%,
    rgba(255, 255, 255, 0.3) 50%,
    transparent 100%
  );
  animation: progressShine 2s ease-in-out infinite;
}

@keyframes progressShine {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(100%); }
}

/* === 比例指示区域 === */
.ratioSection {
  display: flex;
  justify-content: space-around;
  padding-top: 8px;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
}

.ratioItem {
  display: flex;
  align-items: center;
  gap: 6px;
}

.ratioDot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  box-shadow: 0 0 6px currentColor;
}

.ratioLabel {
  font-size: 11px;
  color: rgba(255, 255, 255, 0.5);
}

.ratioValue {
  font-size: 12px;
  font-weight: 600;
  color: rgba(255, 255, 255, 0.9);
}
```

---

## 阶段 6：工资计算器

### 6.1 Rust 工资计算模块

**src-tauri/src/salary/calculator.rs**

```rust
use chrono::{DateTime, Utc, Datelike, NaiveTime};
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SalaryConfig {
    pub monthly_salary: f64,
    pub work_days_per_month: u32,
    pub work_hours_per_day: f64,
    pub work_start_time: String,  // "HH:MM"
    pub work_end_time: String,    // "HH:MM"
    pub currency: String,
}

#[derive(Debug, Serialize)]
pub struct SalaryInfo {
    pub daily_salary: f64,        // 日薪
    pub hourly_salary: f64,       // 时薪
    pub today_earnings: f64,      // 今日已赚
    pub month_earnings: f64,      // 本月已赚
    pub countdown_seconds: i64,   // 距下班秒数
    pub work_progress: f64,       // 今日工作进度百分比
}

impl SalaryConfig {
    pub fn calculate_daily_salary(&self) -> f64 {
        self.monthly_salary / self.work_days_per_month as f64
    }

    pub fn calculate_hourly_salary(&self) -> f64 {
        self.calculate_daily_salary() / self.work_hours_per_day
    }

    pub fn calculate_current_status(&self) -> SalaryInfo {
        let daily = self.calculate_daily_salary();
        let hourly = self.calculate_hourly_salary();

        let now = Utc::now();
        let start = parse_time(&self.work_start_time);
        let end = parse_time(&self.work_end_time);

        // 计算今日工作时间
        let work_start = now.with_hour(start.0).unwrap()
                           .with_minute(start.1).unwrap()
                           .with_second(0).unwrap();
        let work_end = now.with_hour(end.0).unwrap()
                         .with_minute(end.1).unwrap()
                         .with_second(0).unwrap();

        // 计算已工作时间(小时)
        let hours_worked = if now < work_start {
            0.0
        } else if now > work_end {
            self.work_hours_per_day
        } else {
            (now - work_start).num_seconds() as f64 / 3600.0
        };

        // 今日已赚
        let today_earnings = hours_worked * hourly;

        // 本月已赚 (简化计算: 已过工作日 × 日薪)
        let days_passed = count_work_days_this_month(now, self.work_days_per_month);
        let month_earnings = days_passed as f64 * daily;

        // 下班倒计时
        let countdown = if now < work_end {
            (work_end - now).num_seconds()
        } else {
            0
        };

        // 工作进度
        let progress = (hours_worked / self.work_hours_per_day * 100.0).min(100.0);

        SalaryInfo {
            daily_salary: daily,
            hourly_salary: hourly,
            today_earnings,
            month_earnings,
            countdown_seconds: countdown,
            work_progress: progress,
        }
    }
}

fn parse_time(time_str: &str) -> (u32, u32) {
    let parts: Vec<&str> = time_str.split(':').collect();
    (parts[0].parse().unwrap(), parts[1].parse().unwrap())
}

fn count_work_days_this_month(now: DateTime<Utc>, total_work_days: u32) -> u32 {
    let current_day = now.day();
    // 简化: 假设均匀分布
    ((current_day as f64 / 30.0) * total_work_days as f64).floor() as u32
}
```

### 6.2 工资设置 UI

**src/components/SalaryCalc/SalaryForm.tsx**

```tsx
import { useState, useEffect } from 'react';
import { invoke } from '@tauri-apps/api/core';
import styles from './styles.module.css';

interface SalaryConfig {
  monthly_salary: number;
  work_days_per_month: number;
  work_hours_per_day: number;
  work_start_time: string;
  work_end_time: string;
}

export function SalaryForm() {
  const [config, setConfig] = useState<SalaryConfig>({
    monthly_salary: 15000,
    work_days_per_month: 22,
    work_hours_per_day: 8,
    work_start_time: '09:00',
    work_end_time: '18:00',
  });

  const [info, setInfo] = useState({
    daily_salary: 0,
    hourly_salary: 0,
  });

  useEffect(() => {
    loadConfig();
  }, []);

  async function loadConfig() {
    const saved = await invoke<SalaryConfig>('get_salary_config');
    setConfig(saved);
    calculate(saved);
  }

  function calculate(c: SalaryConfig) {
    const daily = c.monthly_salary / c.work_days_per_month;
    const hourly = daily / c.work_hours_per_day;
    setInfo({ daily_salary: daily, hourly_salary: hourly });
  }

  async function handleSave() {
    await invoke('save_salary_config', { config });
    calculate(config);
  }

  function handleChange(field: keyof SalaryConfig, value: string | number) {
    const newConfig = { ...config, [field]: value };
    setConfig(newConfig);
    calculate(newConfig);
  }

  return (
    <div className={styles.form}>
      <h3>工资配置</h3>

      <div className={styles.field}>
        <label>月薪 (元)</label>
        <input
          type="number"
          value={config.monthly_salary}
          onChange={e => handleChange('monthly_salary', Number(e.target.value))}
        />
      </div>

      <div className={styles.field}>
        <label>月工作日</label>
        <input
          type="number"
          value={config.work_days_per_month}
          onChange={e => handleChange('work_days_per_month', Number(e.target.value))}
        />
      </div>

      <div className={styles.field}>
        <label>每日工作时长 (小时)</label>
        <input
          type="number"
          step="0.5"
          value={config.work_hours_per_day}
          onChange={e => handleChange('work_hours_per_day', Number(e.target.value))}
        />
      </div>

      <div className={styles.row}>
        <div className={styles.field}>
          <label>上班时间</label>
          <input
            type="time"
            value={config.work_start_time}
            onChange={e => handleChange('work_start_time', e.target.value)}
          />
        </div>
        <div className={styles.field}>
          <label>下班时间</label>
          <input
            type="time"
            value={config.work_end_time}
            onChange={e => handleChange('work_end_time', e.target.value)}
          />
        </div>
      </div>

      <div className={styles.result}>
        <div className={styles.resultItem}>
          <span>日薪</span>
          <strong>¥{info.daily_salary.toFixed(2)}</strong>
        </div>
        <div className={styles.resultItem}>
          <span>时薪</span>
          <strong>¥{info.hourly_salary.toFixed(2)}</strong>
        </div>
      </div>

      <button className={styles.saveBtn} onClick={handleSave}>
        保存设置
      </button>
    </div>
  );
}
```

---

## 阶段 7：主窗口统计面板

**src/components/Dashboard/index.tsx**

```tsx
import { useState, useEffect } from 'react';
import { invoke } from '@tauri-apps/api/core';
import { LineChart, Line, BarChart, Bar, PieChart, Pie, Cell } from 'recharts';
import styles from './styles.module.css';

export function Dashboard() {
  const [activeTab, setActiveTab] = useState<'today' | 'history' | 'apps'>('today');
  const [todayStats, setTodayStats] = useState({ total: 0, charCount: 0, codeCount: 0 });
  const [hourlyData, setHourlyData] = useState<{ hour: string; count: number }[]>([]);
  const [historyData, setHistoryData] = useState<{ date: string; total: number }[]>([]);
  const [appBreakdown, setAppBreakdown] = useState<{ name: string; value: number }[]>([]);

  useEffect(() => {
    loadStats();
  }, [activeTab]);

  async function loadStats() {
    if (activeTab === 'today') {
      const stats = await invoke('get_today_stats');
      setTodayStats(stats);

      const hourly = await invoke('get_today_hourly');
      setHourlyData(hourly);
    } else if (activeTab === 'history') {
      const history = await invoke('get_history_stats', { days: 30 });
      setHistoryData(history);
    } else {
      const apps = await invoke('get_app_breakdown');
      setAppBreakdown(apps);
    }
  }

  const COLORS = ['#4ade80', '#60a5fa', '#f472b6', '#facc15', '#a78bfa'];

  return (
    <div className={styles.dashboard}>
      <div className={styles.tabs}>
        <button
          className={activeTab === 'today' ? styles.active : ''}
          onClick={() => setActiveTab('today')}
        >
          今日统计
        </button>
        <button
          className={activeTab === 'history' ? styles.active : ''}
          onClick={() => setActiveTab('history')}
        >
          历史趋势
        </button>
        <button
          className={activeTab === 'apps' ? styles.active : ''}
          onClick={() => setActiveTab('apps')}
        >
          应用分布
        </button>
      </div>

      <div className={styles.content}>
        {activeTab === 'today' && (
          <>
            <div className={styles.statsCards}>
              <div className={styles.card}>
                <span className={styles.cardLabel}>今日按键</span>
                <span className={styles.cardValue}>{todayStats.total.toLocaleString()}</span>
              </div>
              <div className={styles.card}>
                <span className={styles.cardLabel}>字符输入</span>
                <span className={styles.cardValue}>{todayStats.charCount.toLocaleString()}</span>
              </div>
              <div className={styles.card}>
                <span className={styles.cardLabel}>代码编写</span>
                <span className={styles.cardValue}>{todayStats.codeCount.toLocaleString()}</span>
              </div>
            </div>
            <div className={styles.chart}>
              <h4>今日按键分布</h4>
              <BarChart data={hourlyData} width={600} height={200}>
                <Bar dataKey="count" fill="#4ade80" radius={[4, 4, 0, 0]} />
              </BarChart>
            </div>
          </>
        )}

        {activeTab === 'history' && (
          <div className={styles.chart}>
            <h4>最近30天趋势</h4>
            <LineChart data={historyData} width={600} height={300}>
              <Line type="monotone" dataKey="total" stroke="#60a5fa" strokeWidth={2} />
            </LineChart>
          </div>
        )}

        {activeTab === 'apps' && (
          <div className={styles.chart}>
            <h4>应用按键占比</h4>
            <PieChart width={400} height={300}>
              <Pie data={appBreakdown} dataKey="value" cx={200} cy={150} outerRadius={100}>
                {appBreakdown.map((_, index) => (
                  <Cell key={index} fill={COLORS[index % COLORS.length]} />
                ))}
              </Pie>
            </PieChart>
            <div className={styles.legend}>
              {appBreakdown.map((item, index) => (
                <span key={item.name} className={styles.legendItem}>
                  <span style={{ background: COLORS[index % COLORS.length] }} />
                  {item.name}: {item.value}
                </span>
              ))}
            </div>
          </div>
        )}
      </div>
    </div>
  );
}
```

---

## 阶段 8：系统托盘 + 开机自启

### 8.1 托盘图标

**src-tauri/src/main.rs**

```rust
use tauri::{
    menu::{Menu, MenuItem},
    tray::{MouseButton, MouseButtonState, TrayIconBuilder, TrayIconEvent},
    Manager,
};

fn main() {
    tauri::Builder::default()
        .setup(|app| {
            // 创建托盘菜单
            let show_widget = MenuItem::with_id(app, "show_widget", "显示悬浮窗", true, None::<&str>)?;
            let show_main = MenuItem::with_id(app, "show_main", "打开主窗口", true, None::<&str>)?;
            let quit = MenuItem::with_id(app, "quit", "退出", true, None::<&str>)?;

            let menu = Menu::with_items(app, &[&show_widget, &show_main, &quit])?;

            // 创建托盘图标
            let _tray = TrayIconBuilder::new()
                .icon(app.default_window_icon().unwrap().clone())
                .menu(&menu)
                .menu_on_left_click(false)
                .on_menu_event(|app, event| match event.id.as_ref() {
                    "show_widget" => {
                        app.get_webview_window("floating").unwrap().show().unwrap();
                    }
                    "show_main" => {
                        let win = app.get_webview_window("main").unwrap();
                        win.show().unwrap();
                        win.set_focus().unwrap();
                    }
                    "quit" => {
                        app.exit(0);
                    }
                    _ => {}
                })
                .on_tray_icon_event(|tray, event| {
                    if let TrayIconEvent::Click {
                        button: MouseButton::Left,
                        button_state: MouseButtonState::Up,
                        ..
                    } = event {
                        let app = tray.app_handle();
                        let win = app.get_webview_window("floating").unwrap();
                        win.show().unwrap();
                    }
                })
                .build(app)?;

            // 启动键盘监听
            let listener = keyboard::KeyboardListener::new(app.handle().clone());
            listener.start()?;

            Ok(())
        })
        .run(tauri::generate_context!())
        .expect("启动应用失败");
}
```

### 8.2 开机自启

**src-tauri/src/autostart.rs**

```rust
#[cfg(target_os = "windows")]
pub fn enable_autostart() -> Result<(), Box<dyn std::error::Error>> {
    use winapi::um::winreg::{RegSetValueExW, HKEY_CURRENT_USER};
    use std::ffi::OsStr;
    use std::os::windows::ffi::OsStrExt;

    let exe_path = std::env::current_exe()?;
    let path: Vec<u16> = OsStr::new(&exe_path.to_string_lossy())
        .encode_wide()
        .chain(std::iter::once(0))
        .collect();

    // 添加到注册表启动项
    unsafe {
        let key = winapi::um::winreg::RegOpenKeyExW(
            HKEY_CURRENT_USER,
            "Software\\Microsoft\\Windows\\CurrentVersion\\Run\0".encode_utf16().collect::<Vec<_>>().as_ptr(),
            0,
            winapi::um::winnt::KEY_SET_VALUE,
            &mut 0,
        );
        RegSetValueExW(key, "KeyboardTracker\0".encode_utf16().collect::<Vec<_>>().as_ptr(), 0, 1, path.as_ptr() as _, (path.len() * 2) as u32);
    }

    Ok(())
}

#[cfg(target_os = "macos")]
pub fn enable_autostart() -> Result<(), Box<dyn std::error::Error>> {
    // macOS 使用 LaunchAgent
    let plist_path = dirs::home_dir()
        .unwrap()
        .join("Library/LaunchAgents/com.yourname.keyboard-tracker.plist");

    // 创建 plist 文件...
    Ok(())
}
```

---

## 阶段 9：macOS 适配要点

```rust
// macOS 特殊处理
#[cfg(target_os = "macos")]
mod macos_specific {
    use tauri::Manager;

    // 1. 辅助功能权限检查
    pub fn check_permissions() -> bool {
        let trusted = unsafe { AXIsProcessTrusted() };
        trusted
    }

    // 2. 请求权限
    pub fn request_permissions() {
        let options = unsafe {
            AXIsProcessTrustedWithOptions(nullptr, kAXTrustedCheckOptionPrompt)
        };
    }

    // 3. 菜单栏应用模式 (不显示在 Dock)
    pub fn setup_menu_bar_app(app: &tauri::AppHandle) {
        app.set_activation_policy(tauri::ActivationPolicy::Accessory);
    }
}
```

---

## 阶段 10：打包配置

**tauri.conf.json (bundle 部分)**

```json
{
  "bundle": {
    "active": true,
    "targets": ["msi", "dmg"],
    "identifier": "com.yourname.keyboard-tracker",
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ],
    "windows": {
      "wix": {
        "language": "zh-CN"
      }
    },
    "macOS": {
      "minimumSystemVersion": "10.13",
      "entitlements": null,
      "exceptionDomain": ""
    }
  }
}
```
